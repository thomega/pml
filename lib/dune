(library
  (name pml)
  (public_name pml)
  (modules
    Musicbrainz Work Discid Query Cache)
  (private_modules
    )
  ; (wrapped false)
  (inline_tests)
  (preprocess (pps ppx_inline_test))
  ; (flags (:standard -I +unix))
  ; (ocamlc_flags (:standard))
  (ocamlopt_flags (:standard -inline 32))
  (libraries clibdiscid curl jsont jsont.bytesrw re))

(documentation
  (package pml)
  (mld_files index))
